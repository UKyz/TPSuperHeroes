/* -- const chai = require('chai');

const {City} = require('../sls/classes/city.js');

chai.should();

describe('city.js', () => {
  const city1 = new City('Roscoff', -3, 48);

  it('should create a city', () => {
    city1.name.should.be.equal('Roscoff');
    city1.longitude.should.be.equal(-3);
    city1.latitude.should.be.equal(48);
  });

  it('should give the localization of the city', () => {
    city1.localization.longitude.should.be.equal(-3);
    city1.localization.latitude.should.be.equal(48);
  });
}); */

